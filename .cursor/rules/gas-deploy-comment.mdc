---
description: 提供 GAS 部署時的 Comment for deploy
globs: ["**/*.gs", "gas-backend/**/*.gs"]
alwaysRun: false
---

# GAS 部署 Comment 規則

凡修改 `gas-backend/**/*.gs` 檔案後，在回覆**結尾**需提供 **Comment for deploy**，格式如下：

---

## Comment for deploy 格式

**格式：** `v{版本號}：{簡短描述}`

**範例：**
```
## Comment for deploy
v0.6.32：班表上傳時透過人員 sheet 將員工名稱轉成打卡名稱後再寫入。
```

**範例 2：**
```
## Comment for deploy
v0.6.35：loadSchedule / loadAttendance 支援 startDate、endDate 日期區間。
```

---

此 comment 將用於 GAS 專案的「管理版本」→「說明」欄位，供日後追蹤與回溯。
